# 放置换装 · 进度跟踪

> 透明无边框的桌面放置换装游戏，目标平台 Steam
> 最后更新：2026-07-23

---

## 一、项目概况

| 项 | 内容 |
|---|---|
| 项目名称 | 放置换装（暂定） |
| 类型 | 治愈系 + 放置（Idle） + 换装 |
| 目标平台 | Steam（Windows / Mac / Linux） |
| 发行方式 | 单机买断 / 免费 + DLC（待定） |
| 当前阶段 | **四界面重构阶段 0–4 全部完成**（见 `四界面切换-重构计划.md`）；代码审查 P0/P1/P2 全部落地；**剩余：Godot 4.7 实机验收 + 离线收益 + Steam 集成** |
| 概念图 | `放置换装方案-2.png` |
| 代码仓库 | https://github.com/ChrisUtada/fz.git （已连接，默认分支 `main`，SSH 推送；最新 `c73680d`） |
| 关联文档 | `四界面切换-重构计划.md`（当前主玩法蓝图）· `代码审查报告.md`（P0/P1/P2 追踪）· `键鼠统计优化方案.md`（外出键鼠 GDExtension 方案） |

---

## 一点五、工程规范（最佳实践）  ⚠️ 全局强制

> 从阶段 1 起，所有代码必须遵守以下原则。违反者重构。

### 核心原则

| 原则 | 含义 | 在本项目中的体现 |
|---|---|---|
| **组合优于继承** | 通过组合节点/组件构建行为，而非深继承链 | Customer = Sprite2D + Area2D + 状态脚本，不继承"EntityBase" |
| **低耦合** | 模块间依赖尽可能少、通过信号/接口通信 | Customer 只发 `order_completed` 信号，不直接调用 GameManager |
| **高内聚** | 每个模块只做一件事 | GameManager 管货币 / Customer 管交互 / HUD 管 UI / Main 负责编排 |
| **单一职责 (SRP)** | 一个类/脚本只有一个变更理由 | 不在 Customer 里写 HUD 更新逻辑 |
| **依赖倒置 (DI)** | 高层模块不依赖低层实现，依赖抽象 | Spawner 通过参数注入 spawn 目标节点，而非硬编码路径 |
| **开闭原则 (OCP)** | 对扩展开放，对修改关闭 | 新增顾客类型只需加 JSON + 新场景，不改现有代码 |

### Godot 特定实践

- **信号优先于直接引用**：跨节点通信一律用 `signal` + `connect()`，避免 `get_node("...")` 拿外部引用
- **autoload 最小化**：GameManager 是唯一 autoload，仅持有全局状态；业务逻辑不在 autoload 中
- **数据-外观分离**：Resource 描述"这是什么"（纯数据：贴图引用/裁剪/奖励/权重），场景描述"长什么样"（表现）。顾客数据用 `CustomerData`、衣服数据用 `ClothesData`，场景通过 `apply_data(res)` 消费 Resource 并变现外观 —— 数据不依赖场景，场景不含硬编码数值
- **数据驱动配置**：游戏数据用 Godot **Resource（.tres）** 管理，而非 JSON。新增内容只需加一个 .tres 资源并拖入资源池（如 Main 的 `customer_pool`），不改代码（OCP）；可直接在编辑器预览、持有 Texture/资源引用、类型安全
- **场景树即架构图**：父子关系 = 组合关系；信号连接 = 发布-订阅
- **避免全局状态污染**：除 GameManager 的货币字段外，其余状态封装在各自节点内

---

## 二、已确认的技术决策

| 决策点 | 选择 | 备注 |
|---|---|---|
| 引擎 | **Godot 4.7** | 导出友好、单文件打包 |
| 窗口形态 | **完全无边框 + 透明背景** | `borderless=true` + `transparent=true` |
| 美术节奏 | 先跑通玩法，素材逐步产出 | 先用程序化/占位图 |
| 游戏数据 | `data/*.tres`（Godot **Resource**） | 衣服/顾客/装饰/配方，纯数据模型，场景消费控制外观 |
| 玩家存档 | `user://save.tres`（Godot Resource） | 含离线时间戳，支持 Steam Cloud |
| 用户设置 | `user://settings.cfg`（ConfigFile） | 透明度/音量/置顶 |
| Steam 集成 | GodotSteam 插件（后期阶段 5） | 成就/云存档/workshop 预备 |
| 导出格式 | Windows Desktop + Embed PCK | 单文件 `.exe` |

---

## 三、开发阶段计划

> **重要**：项目自 2026-07 起已按 `四界面切换-重构计划.md` 重构为「家园/种植/换装/工坊 四对等主 tab + 仓库全局浮层」结构。下方原始阶段 0–3 均已完成；重构计划阶段 0–4 亦全部落地。**当前唯一系统性缺口 = Godot 4.7 实机验收 + 离线收益 + Steam 集成**。

### 阶段 0 · 项目骨架  ✅ 完成
- [x] 初始化 Godot 4.7 项目、无边框 + 透明窗口、工程目录结构、main 透明面板 + 拖拽 + × 关闭
- [x] 窗口右下角锁定（`_position_to_bottom_right`）；顶部 30px 拖拽带（`TOP_DRAG_BAND`，避免与换装拖衣冲突）

### 阶段 1 · 核心循环可玩  ✅ 完成
- [x] 顾客自动生成 + 点击/购买反馈 + HUD 金币/灵感数字动画（观察者模式监听 `currency_changed`）
- [x] 顾客经济已升级为「购买展架服装后离开」（见重构阶段 4），非早期「点一下给钱」

### 阶段 2 · 数据驱动（Resource 优先）  ✅ 完成
- [x] Resource 数据层：`CustomerData` / `PetData` / `ClothesData` / `ActivityData` / `ProductData` / `ItemData`(基类) / `SeedData` / `BlueprintData`，全部 `.tres` 驱动
- [x] 换装场景全套（拖放穿脱、同部位互斥、像素命中脱下、共享原点 736×414 画布对齐）
- [x] 数据统一收口进 `inventory` + `_item_registry`（见重构阶段 0）

### 阶段 3 · 副玩法  ✅ 完成
- [x] **灵感系统（番茄钟/专注活动）**：活动列表 + 计时视图 + 墙钟倒计时 + 完成发灵感；累计灵感 `inspiration_total_earned` 单调递增（驱动蓝图解锁）
- [x] **外出活动（键鼠步数）**：`ActivityManager` + Godot-Global-Input GDExtension 全局键鼠捕获（`get_keys_pressed_detailed()` 差分累加统一「步数」）；导出版真实生效（编辑器/无头下钩子不启动为预期）
- [x] **电话购物 + 订单中心 + 仓库**：多订单并行 + 墙钟离线到货 + 逐件领取入库
- [x] **摆放系统**：`PlacementManager` 自由拖放摆件 + 位置存档（0.5s 节流落盘 + `_exit_tree` 兜底）
- [x] **培育/种植系统**：种植屏 6 槽（苗/成长/成熟三阶段墙钟计时 + 采摘进 CROP）
- [x] **制作系统**：工坊屏「制作」（蓝图→材料→产出）+「分解」（作物→染料/材料）两子页

### 四界面重构（`四界面切换-重构计划.md`）· 阶段 0–4 ✅ 全部完成
- [x] **阶段 0 数据层统一**：`ItemData` 七分类 + `inventory` + `_item_registry` + SeedData/BlueprintData + 蓝图解锁/灵感统计 + 农场槽墙钟状态
- [x] **阶段 1 屏幕框架**：`ScreenManager` 四屏互斥 + 底部 `TabBar` 招牌 + 左右箭头导航 `ArrowNav` + 窗口右下角锁定
- [x] **阶段 2 四屏内容**：换装屏 / 仓库全局浮层（分类筛选 + 增量刷新）/ 种植屏（完整玩法）/ 工坊屏（制作+分解）
- [x] **阶段 3 招牌晃动动画**：点击 Elastic 摆动 + hover 微摆动
- [x] **阶段 4 家园增强**：服装展架 `clothing_rack` + 上架面板 + 顾客购买经济改造 + 桌面装饰花盆限量 + 库存联动
- [ ] **阶段 5 收尾**：仅剩「Godot 4.7 实机自测清单」未逐条实跑验收（见该文档 5.3；进度文档同步=本次已做）

### 代码质量（`代码审查报告.md`）· ✅ 全部完成
- [x] **P0 ×1**：placement 复活 bug 已修
- [x] **P1 ×13**：上帝单例拆分为 6 个子管理器（Economy/Inventory/Farm/Rack/Wardrobe/Activity）+ 批量写盘 + getter 收口 + 强类型 + 信号驱动 等全部落地
- [x] **P2 ×15**：公共工具抽取（`Utils`）+ 屏生命周期规范 + 硬编码常量化 + 类型/数据设计修正 + **统一存档版本框架（`Utils.SAVE_VERSION`）推广至全部 12 个存档**
- [x] **Bug 修复**：金币/灵感当前余额未持久化（`user://currency.cfg` + `_ready_to_persist` 守卫）

### 阶段 4（原）· 放置感与离线收益  ☐ 未完成
- [x] 关窗写存档（各子系统 setter/事件即时或节流落盘，已覆盖）
- [x] 墙钟离线续算（种植/订单/农场按墙钟时间戳续算，天然离线友好）
- [ ] **开窗计算离线收益**（顾客自动营业/挂机产出的离线结算 + 封顶 12h）——尚无显式离线收益结算
- [x] 闲置背景动效（宠物猫行走；叶子摇等更多动效可选）

### 阶段 5（原）· Steam 集成  ☐ 未开始
- [ ] 接入 GodotSteam
- [ ] 成就系统（钩子：FIRST_ORDER 等）
- [ ] Steam Cloud 存档同步（存档已全用 `user://`，为云存档预备）
- [ ] 导出 Windows / Mac / Linux（Windows 导出已验证，Mac/Linux 待做）
- [ ] 商店页素材（1920×1080）

---

## 三点五、剩余未完成任务清单（截至 2026-07-23）

> 玩法系统与代码质量债已基本清零；剩余项按优先级归为三类。

### A. 实机验收（阻塞发布，优先级最高）
- [ ] **Godot 4.7 编辑器/导出版完整实机自测**（`四界面切换-重构计划.md` 5.3 清单逐条跑）：
  - [ ] 四主 tab 切换 + 仓库全局浮层 + 招牌摆动
  - [ ] 种植全链路：买花盆+种子 → 放盆种入 → 三阶段进度 → 采摘进 CROP
  - [ ] 工坊：灵感达阈值解锁蓝图 → 材料齐制作出服装/制作物；作物分解出材料
  - [ ] 家园展架：上架、6 槽重复同款、库存数量、顾客购买后消失加金币、无货离开
  - [ ] **外出键鼠「步数」统计在导出版真实生效**（编辑器/无头下钩子不启动为预期）
  - [ ] 关游戏再开：库存/穿搭/位置/金币灵感/农场墙钟续算全部正确
- [ ] 素材替换：占位 ColorRect/文字 → 正式 PNG（电话、展架、种植屏、工坊屏、仓库图标等）

### B. 放置感与离线收益（原阶段 4 未竟项）
- [ ] **开窗离线收益结算**：目前种植/订单/农场按墙钟天然续算，但无「离线期间顾客自动营业/挂机产出」的显式结算 + 12h 封顶
- [ ] 更多闲置背景动效（叶子摇、光影等；宠物猫行走已实现）

### C. Steam 集成（原阶段 5，未开始）
- [ ] 接入 GodotSteam 插件
- [ ] 成就系统（钩子：FIRST_ORDER 等）
- [ ] Steam Cloud 存档同步（存档已全走 `user://`，已预备）
- [ ] Mac / Linux 导出（Windows 导出已验证）
- [ ] 商店页素材（1920×1080）

### D. 待设计 / 开放项（不阻塞前期）
- [ ] **探索系统**：材料来源之一（购买/分解/种植之外），玩法未设计
- [ ] 花盆槽位扩容（暂定 6）、桌面装饰花盆上限（暂定 3）——具体值后续再定
- [ ] **产品演进：自由摆放花园世界 `GardenScreen`**（种植屏从固定 6 网格 → 花园自由摆放；`farm_slots` 定长数组 → `farm_pots` 字典按实例 id 寻址；素材字段 `SeedData.sprout/growing/mature_tex`、`ProductData.garden_placement` 已预留）
- [ ] 发售价位 / 买断 vs 免费+DLC、中英文双语、BGM/音效方案（见「六、待确认」）

---

## 四、Godot 4 关键配置片段

**project.godot（窗口）**
```ini
[display]
window/size/viewport_width=680
window/size/viewport_height=420
window/per_pixel_transparency/allowed=true
window/per_pixel_transparency/enabled=true
```

**main.gd（无边框透明）**
```gdscript
extends Node

func _ready() -> void:
    var win := get_window()
    win.mode = Window.MODE_WINDOWED
    win.borderless = true
    win.transparent = true
    win.always_on_top = false
    win.position = Vector2i(1280, 720)
```

---

## 五、进度日志

### 2026-07-18
- 查看概念图 `放置换装方案-2.png`，确认游戏类型与画风
- 与用户确认技术决策：Godot 4 + 透明无边框 + 逐步出素材 + Steam 发行
- 确定文件存储策略：游戏数据用 JSON，玩家存档用 Resource，设置用 ConfigFile
- 制定 6 阶段开发路线（阶段 0~5）
- 创建本进度跟踪文档
- **连接 GitHub 仓库** `ChrisUtada/fz`：本地 `git init -b main`，添加 `.gitignore`（Godot 生成目录/导出/工具数据），首次提交 `3880c6f` 并推送 `main` 分支（SSH 透明推送，避免凭据弹窗）
- **阶段 0 工程骨架**：创建 `project.godot`（Godot 4.7、GL Compatibility、窗口 680×420、透明无边框配置）、`icon.svg`、`scenes/main.tscn`（透明面板+标题+×关闭）、`scripts/main.gd`（borderless+transparent 运行时设置 + 拖拽移动 + 关闭）、`data/assets/addons` 占位目录。代码完成，待 Godot 4.7 编辑器验证窗口效果
- **阶段 1 核心循环**：
  - 新增 **工程规范** 章节（一点五节）：强制执行组合优于继承、低耦合高内聚、单一职责(SRP)、依赖倒置(DI)、开闭原则(OCP)、信号解耦、autoload 最小化等最佳实践
  - 注册 `GameManager` autoload 单例（`project.godot`）：持有 gold/inspiration 货币状态，`add_gold/add_inspiration/complete_order` 方法，发射 `currency_changed` 信号
  - 创建 `scenes/customer.tscn` + `scripts/customer.gd`：组合式顾客节点（Sprite2D char.png + Area2D 点击区），点击 emit `order_completed` 信号，缩放弹跳+淡出自毁动画，不引用 GameManager/HUD（低耦合）
  - 创建 `scenes/hud.tscn` + `scripts/hud.gd`：顶部半透明货币栏，观察者模式订阅 `currency_changed`，Tween `tween_method` 实现数字滚动动画
  - 重构 `scenes/main.tscn` + `scripts/main.gd`：编排层，组合 HUD + PlayArea(Node2D)，`_spawn_customer()` 定时生成并注入随机奖励，`_on_order_completed()` 委托 GameManager → 安排下一轮
  - 使用 `assets/char.png`（736×414 手绘简笔小人）作为顾客形象，scale=0.38
- **阶段 1 补·背景接入**：`main.tscn` 新增底层 `Background`(TextureRect) 全窗口铺满 `assets/BG.png`（736×414）；`Panel`(ColorRect) 改为全窗口透明容器（alpha=0）仅承载 UI；`PlayArea` 居中到 (340,220)
- **阶段 1 补·架构决策：顾客回退 Node2D**：曾因点击命中框未覆盖偏右小人而临时改用 `TextureButton`，用户要求更高可扩展性后**回退为 Node2D 组合**（Sprite2D + Area2D 命中框覆盖整张贴图）。拖拽冲突改用「Area2D `mouse_entered/exited` 悬停守卫」解决（点击顾客时不拖窗口），不再依赖 Control 的 `mouse_filter`；后续可直接挂 `AnimationPlayer`/`AnimationTree` 做换装与动作
- **阶段 1 修·点击无响应（真正根因）commit `3bac5d7`**：① 输入层此前已重构为 `Main._unhandled_input` 世界坐标命中测试（方向正确）；② **真因与输入系统无关**：`customer.tscn` 的 Sprite 用 `region_rect` 仅裁出小人且 `position=(156,21)`，而 `contains_point` 仍以原点(0,0)为中心用 280×157 矩形检测 → 点小人局部 x≈156>半宽140 **永远 miss** → 静默走拖拽分支（窗口不动）看似无反应；③ 修复：`Sprite.position`→(0,0) 居中，`contains_point` 改基于 `_sprite.get_rect()`（含 region+scale）严格对齐 + 36px 容差，`BASE_SCALE` 0.38→1.0 放大显示，`main.gd` 加 `[click]` 调试输出（验证后移除）。**经验：Sprite+region 时命中框必须用 `get_rect()` 而非手算矩形**

### 阶段 2 补·数据由 Resource 管理（用户纠偏）
- **纠偏**：原阶段 2 规划用 `clothes.json`/`customers.json` 管数据 → 用户指出应使用 **Godot Resource（.tres）** 管理数据、由场景控制外观（数据-外观分离）。已更新技术决策表与工程规范（新增"数据-外观分离"原则，数据驱动配置改 .tres）
- 新建数据层：`scripts/resources/customer_data.gd`（`class_name CustomerData extends Resource`，字段含 texture/region_rect/base_scale/奖励区间/spawn_weight）、`scripts/resources/clothes_data.gd`（`class_name ClothesData extends Resource`，含 slot 枚举/price/inspiration_value/equip_offset）
- 新建示例资源：`data/customer_char.tres`（char.png，region `Rect2(521,145.5,76,154)`，奖励 100/10）、`data/clothes_hat.tres`（占位换装数据）
- 改造 `customer.gd`：新增 `apply_data(data)` → `_apply_visuals()`（设 sprite.texture/region/scale）+ `_roll_reward()`（按数据区间随机奖励）；支持 add_child 前后任意时机调用（`_data_pending` 延迟到 `_ready` 落地）
- 改造 `main.gd`：`_spawn_customer` 从 `customer_pool: Array[CustomerData]`（@export）随机抽取并 `apply_data`；删除旧的 `base_gold_reward/inspiration/reward_variance` 硬编码奖励变量（数据已移交 Resource）。未拖资源时回退到顾客 `@export` 默认值，仍可用
- **待办**：在 Godot 编辑器把 .tres 拖入 Main 的 `customer_pool` / `pet_pool` 才能真正启用随机数据；`class_name` 需编辑器打开工程扫描一次以全局识别
- **宠物同步 Resource 化（用户补充）**：`pet` 也需 Resource 管理。新建 `scripts/resources/pet_data.gd`（`class_name PetData`：texture/region_rect/base_scale/walk_speed/bob_amplitude/bob_frequency/edge_margin/spawn_weight）、`data/pet_cat.tres`（cat.png, region `Rect2(184,243.5,79,36)`）；`pet.gd` 新增 `apply_data()`→`_apply_visuals()`（设 sprite + 覆盖行走参数 @export），`_data_pending` 兼容 add_child 前后；`main.gd` `_spawn_pet` 从 `pet_pool: Array[PetData]`（@export）随机抽取 `apply_data`。顾客/宠物/衣服三类数据现已统一 Resource 驱动

### 2026-07-20
- **灵感系统（番茄钟/专注活动）实现**：
  - 新增数据层 `scripts/resources/activity_data.gd`（`class_name ActivityData extends Resource`，纯数据：activity_name/icon/default_duration_minutes/inspiration_per_minute/description）
  - 新增示例资源 `data/activity_reading.tres`（阅读，默认 25 分 / +10 灵感；奖励值检查器自由填入）
  - 新增弹窗 `scenes/inspiration_panel.tscn` + `scripts/inspiration_panel.gd`：列表视图（从 `activity_pool` 数据驱动生成活动按钮）+ 计时视图（HSlider + SpinBox 双向绑定设时，墙钟时间戳倒计时）；× 关闭 queue_free；完成 emit `activity_completed({"inspiration": N})`
  - 入口集成：`ui_panel.gd`/`scenes/ui_panel.tscn` 加「灵感」按钮（与「换装」并列）emit `inspiration_requested`；`main.gd` 加 `@export activity_pool`、`_open_inspiration()`（activity_pool 为空时自动加载 reading.tres 回退）、`_on_activity_completed`（调 `GameManager.add_inspiration`）、`_has_inspiration()` 输入守卫
  - 复用 `GameManager.add_inspiration` 与 `HUD`（观察者自动滚动），零改动
  - 技术决策（用户确认）：奖励值放 ActivityData .tres、墙钟时间戳计时、中途关弹窗中止不发奖、时间范围 1~60 分钟
  - **待验证**：用 Godot 4.7 打开工程，确认「灵感」按钮出现、弹窗弹出、阅读计时与发奖、HUD 数字滚动

### 2026-07-19
- **换装场景实现**：
  - 素材分析：`assets/clothes/` 含 zz.png(人物底图 194×307) + 5件衣服各 a/b 两图（a=穿着态 194×307 共享画布裁切，b=背包预览 60×57）
  - 扩展 ClothesData：新增 `icon_texture: Texture2D` 字段（b 图预览），`texture` 语义为 a 图穿着态
  - 创建 5 件 .tres 资源：`clothes_hat.tres`(HEAD), `clothes_shirt.tres`(BODY), `clothes_skirt.tres`(BODY), `clothes_shoe_tres`(FEET), `clothes_gt_tres`(ACCESSORY)
  - 构建 wardrobe 场景体系：
    - `scripts/wardrobe.gd`：编排层，从 clothes_pool 生成背包格子、管理装备状态字典 `_equipped`
    - `scripts/clothes/item_slot.gd`：背包格子(TextureButton)，`pressed` 由 Wardrobe 连接 → 单击装备/脱下；不再支持拖拽
    - `scripts/clothes/equip_target.gd`：人物区(Control)，仅处理「轻点已装备部位 → 请求脱下」(`clothes_unequip_requested`)；拖放接收逻辑已移除
    - `scenes/wardrobe.tscn`：CharacterArea(left,360w) + EquipLayers(4×Sprite2D) + BackpackArea(right,320w,GridContainer 3列) + CloseButton
  - 集成入口：UI Panel Content 加「换装」Button → emit `wardrobe_requested` → Main._open_wardrobe() 实例化覆盖层
  - 输入守卫：Main._input 加 `_has_wardrobe()` 检查，换装打开时禁止空白窗口拖拽
  - **equip_offset 初始值全部设 (0,0)**——a 图与 zz 共享 194×307 画布，可能已对齐；需在编辑器中验证并根据实际效果微调

### 2026-07-21 ~ 07-23（重构与代码质量收官，摘要）
- **四界面重构落地**（`四界面切换-重构计划.md` 阶段 0–4）：数据层统一 inventory + ItemData 七分类 + SeedData/BlueprintData；ScreenManager 四屏互斥 + 底部 TabBar 招牌 + ArrowNav 箭头导航；换装/仓库浮层/种植/工坊四屏内容；招牌 Elastic + hover 摆动动画；家园服装展架 + 顾客购买经济改造 + 装饰花盆限量
- **衣橱/仓库/展架三权分立**：衣橱=收藏(`unlocked_clothes`)、仓库=数量(`inventory`)、展架=可售；根除「卖光后显示幽灵衣」与「脱下变可售」两 bug
- **外出键鼠统计改 GDExtension**：废弃 Python 子进程方案，接入 Godot-Global-Input 预编译 dll（进程内常驻、导出版真实生效），统一「步数」计数
- **代码审查全清**：P0×1 + P1×13（含上帝单例拆分为 6 子管理器）+ P2×15（Utils 公共工具、屏生命周期、硬编码常量化、类型/数据设计、统一存档版本框架推广至 12 个存档）全部落地推送
- **Bug 修复**：金币/灵感当前余额未持久化（新增 `user://currency.cfg` + `_ready_to_persist` 守卫，防初始化阶段反向覆盖）
- **进度文档同步（本次）**：更新阶段状态至真实进度，新增「三点五、剩余未完成任务清单」（实机验收 / 离线收益 / Steam 集成 / 待设计项）
- 最新提交 `c73680d`，工作树与 origin/main 同步

---

## 六、待确认 / 问题清单

| # | 问题 | 状态 |
|---|---|---|
| 1 | 发售价位 / 买断还是免费+DLC | 未定 |
| 2 | 是否需要中英文双语（Steam 全球发行） | 未定 |
| 3 | 离线收益封顶时间（建议 12h） | 待确认 |
| 4 | 是否要做 Steam 创意工坊（玩家自定义衣服） | 未定 |
| 5 | 背景音乐 / 音效方案 | 未定 |

---

## 七、关键经验 / 备注

- Godot 4 的 `transparent=true` 必须配合 `per_pixel_transparency/allowed=true`，否则只是硬切到窗口背景色
- 放置游戏的存档核心是"最后离线时间"字段，用于离线收益计算
- 存档路径用 `user://` 才能挂上 Steam Cloud
- 早期就留好 Steam 成就钩子，避免后期返工

### 2026-07-24
- **换装交互：拖拽 → 单击**（用户决策：点击式换装，单击优于双击）
  - `item_slot.gd`：移除 `_get_drag_data` 拖拽源，格子纯点击；`pressed` 由 Wardrobe 连接处理装备/脱下
  - `equip_target.gd`：移除 `_can_drop_data`/`_drop_data`/`clothes_dropped` 拖放接收逻辑，仅保留「轻点角色已装备部位 → 请求脱下」(`clothes_unequip_requested`)
  - `wardrobe.gd`：`_populate_backpack` 给每个格子 `slot.pressed.connect(_on_slot_clicked.bind(item_data))`；新增 `_on_slot_clicked(data)`——`GameManager.is_worn(data.id)` 为真则 `_unequip(data.slot)`，否则 `_equip(data)`（单击 toggle 穿脱）；删除 `_on_clothes_dropped` 及 `clothes_dropped` 连接
  - 行为：背包单击装备、已穿戴格子再单击脱下；角色身上轻点仍可脱下（双通道）。无头解析 + 12s 冒烟 0 错。**未提交**。
