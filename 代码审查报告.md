# fz 项目代码审查报告

> 审查范围：Godot 4.7 桌面常驻挂机换装小游戏（窗口 680×420，透明无边框）
> 审查日期：2026-07-23
> 审查方式：核心数据层 `game_manager.gd` 精读 + UI/交互层、数据/逻辑层两路探索代理并发审查 + 所有 P0/P1 关键发现逐条源码验证

---

## 一、总览

| 级别 | 数量 | 已改代码 | 含义 |
|---|---|---|---|
| **P0** | 1 | 1 | 必须修的 bug（数据损坏/崩溃） |
| **P1** | 13 | 13 | 应修的最佳实践 / 隐患 |
| **P2** | 15 | 15（①~⑥ 全部完成；存档版本框架已推广至全部存档） | 可选优化（重复代码 / 一致性） |

> **进度口径（截至 2026-07-23）**：**① P0 + ② 低成本高收益 P1（#6/#7/#10/#11/#13）共 6 处，外加用户追加的「弹窗不阻断窗口拖动」「变量重名」2 处，已于提交 `a862615` 推送 `origin/main`**。另，**④ 上帝单例拆分 P1 #2 已全部完成（6/6 子管理器）**：先后抽出 `InventoryManager` / `EconomyManager` / `ActivityManager` / `WardrobeManager` / `RackManager` / `FarmManager` 为门面式子管理器，`GameManager` 持有并委托转发，全代码库 `GameManager.xxx` 调用点零改动；分别提交 `9e387b7` / `724ac53` / `11972e0` / `2335900` / `e78ddf1` / `74010f9`（含 `economy_manager.gd.uid` 补交），均推送 `origin/main`。其中 `RackManager` 编译期类型推断报错（第 118 行 `avail :=` 退化为 Variant）已修于 `b68d8cd`；**P1 #3 批量写盘优化 `6f605ac`**（6.1c）、**P1 #4 UI 直读字段 getter 收口 `2b66ea8`**（6.1d）、**P1 #5 decompose 弱类型改 `Array[MaterialCost]` `adb797e`**（6.1e）、**P1 #8 衰减系数抽 `get_reward_factor()` `474e180`**（6.1f）、**P1 #9 phone 每帧轮询改订 `arrived_changed` 信号 `3960e1f`**（6.1g）、**P1 #12 删除已废弃损坏的 `align_tool` 开发工具 `ad3f022`**（6.1h）、**P1 #14 focus_bar 去误导性 `@onready` + `await` 后补 `is_instance_valid(self)` 守卫 `f557f77`**（6.1i）均已落地。详见第六节。

**最大的架构债**（已解决）：`game_manager.gd` 曾是个 **1383 行 / 9 职责的上帝单例**（货币、活动、订单、库存、穿搭、蓝图、农场、展架、外出统计全堆一起），违反 SRP。已于 2026-07-23 通过门面式渐进拆分 P1 #2 全部完成——6 个子管理器（Inventory/Economy/Activity/Wardrobe/Rack/Farm）迁出，`GameManager` 退化为门面（仍保留订单与蓝图注册表），见 6.1b。

---

## 二、P0（必须修）

### 1. `placement_manager.gd:63-66` — remove_placed 复活 bug（已验证 · ✅已提交推送 a862615）
`queue_free()` 不会立即把节点移出树，紧接着的 `save_placements()` 遍历 `get_children()` 时被删节点仍在，仍被写入存档 → 下次 `restore()` 复活（摆放物删不掉，越删越多）。

**修法 A（推荐）**：save 时跳过待删节点
```gdscript
for c in _container.get_children():
    if c is Placeable and c.data != null and not c.is_queued_for_deletion():
        # 写入存档
```

**修法 B**：先移出树再释放
```gdscript
remove_child(p)
p.queue_free()
save_placements()
```

---

## 三、P1（应修）

### 架构与耦合

**2. `game_manager.gd` 上帝单例（1383 行 / 9 职责）**
建议按领域拆为独立 autoload：`EconomyManager` / `ActivityManager` / `InventoryManager` / `WardrobeManager` / `FarmManager` / `RackManager`。
成本高但是最重要的长期债务；可渐进式拆，先抽 `InventoryManager` 作为样板。

**3. `game_manager.gd:704-729` craft / `678-697` decompose 批量操作频繁写盘 + 发信号（已验证 · ✅已优化 `6f605ac`）**
一次制作 N 个材料 = N 次 remove + 1 次 add，触发 **N+1 次 `_save_inventory()` 磁盘写 + N+1 次 `inventory_changed.emit()`** → UI 刷新 N+1 次。
建议加 `begin_batch() / commit_batch()` 模式，批量内只存一次、只 emit 一次。
> ✅ 已优化（`6f605ac`）：`InventoryManager` 新增 `begin_batch()/commit_batch()`（depth 计数支持嵌套），`add_item/remove_item` 经 `_notify_inventory_changed()` 在批量期间只置脏；`craft`/`decompose` 整体包在批量段内，落盘与 `inventory_changed` 广播各仅一次。`GameManager` 门面转发 `begin_batch/commit_batch`。

**4. UI 直读 GM 内部字段（违反低耦合 · 已验证 · ✅已优化 `2b66ea8`）**
- `farm_screen.gd:81` 读 `farm_slots[i]`
- `workshop_screen.gd:98` 读 `unlocked_blueprints.has()`
- `wardrobe.gd:92,203` 读 `unlocked_clothes.keys()` / `equipped.keys()`
- （同类）`rack_panel.gd:74` 读 `clothing_rack.size()`

建议补 getter 收口：`get_slot(i)` / `get_equipped_dict()` / `is_blueprint_unlocked(id)` / `get_unlocked_clothing_ids()` 等，UI 只通过接口访问。
> ✅ 已优化（`2b66ea8`）：新增 `get_slot` / `get_unlocked_clothing_ids` / `get_equipped_dict` / `get_rack_count` 四个 getter，分别在 `FarmManager` / `InventoryManager` / `WardrobeManager` / `RackManager` 实现，`GameManager` 门面转发；UI 调用点（`farm_screen` / `wardrobe` / `workshop_screen` / `rack_panel`）全部改为走 getter；原 `equipped` / `unlocked_clothes` / `farm_slots` / `clothing_rack` 四个集合属性降级为兼容 shim（仍保留供过渡）。`is_blueprint_unlocked(id)` 此前已存在（`game_manager.gd:561`），本次仅把 `workshop_screen` 的 `unlocked_blueprints.has()` 直读改为走该 getter。

**5. `game_manager.gd:684-697` decompose 弱类型（已验证 · ✅已优化 `adb797e`）**
`decompose_recipe` 用 `Array[Dictionary]` + id 字符串，每次分解重建 `norm` 字典处理 String/StringName 键；而 `blueprint_data` 用强类型 `MaterialCost`。
统一改 `Array[MaterialCost]` 可删掉整个 workaround（约 20 行）。
> ✅ 已优化（`adb797e`）：`item_data.gd` 的 `decompose_recipe` 由 `Array[Dictionary]` 改为 `Array[MaterialCost]`，并加 `const MaterialCost = preload(...)`（与 `BlueprintData` 同款，解决泛型参数跨文件解析 class_name 问题）；`_decompose_internal` 删掉全部 dict key 归一化、直接遍历 `MaterialCost`（`mc.item.id` / `mc.count`）；`workshop_screen._recipe_text` 直读 `mc.item.display_name` 不再按 id 查表；`data/crops/crop_rose.tres` 的字典数组重写为 `Array[MaterialCost]` 子资源（仿 `blueprint_scarf.tres`）。空 `[]` 的 `fiber.tres` / `red_dye.tres` 无需改动。

### bug / 隐患

**6. `hud.gd:33-34` tween 未 kill（已验证 · ✅已提交推送 a862615）**
注释写「终止旧动画」，实际 `_tween = create_tween()` 没 kill 旧实例，快速货币变化时多条 tween 并行写 `_display_gold` 抖动。
```gdscript
if is_instance_valid(_tween):
    _tween.kill()
_tween = create_tween()
```

**7. `pet.gd:50,52` 初始化顺序定时炸弹（已验证 · ✅已提交推送 a862615）**
`_ready` 里 `_pick_direction()`（用默认 `edge_margin=50`）在 `_apply_visuals()`（覆盖为 `PetData.edge_margin`）之前；若某 PetData.edge_margin<50，定位后 `_check_bounds` 立即判出界 → 瞬灭。当前被 `pet_cat.tres=50` 掩盖。
修：`_ready` 里先 `_apply_visuals()` 再 `_pick_direction()`。

**8. `inspiration_panel.gd:301` 衰减系数与活动逻辑重复**
`maxf(0.5, 1-n*0.2)` 魔法数在 UI（`inspiration_panel.gd`）和 `activity_manager.gd`（`_finish_activity` 结算）双处维护。GM 暴露 `get_reward_factor()`，UI 只读。
> ✅ 已优化（`474e180`）：`activity_manager.gd` 抽 `get_reward_factor()` 为单一真相源，加 `REWARD_DECAY_MIN`/`REWARD_DECAY_STEP` 常量；`_finish_activity` 结算与 `inspiration_panel._update_countdown_label` 预览都走它；`game_manager.gd` 门面转发 `get_reward_factor()`，UI 删掉魔法数。

**9. `phone.gd:113-124` `_process` 每帧轮询 `get_arrived().size()`**
改订 `arrived_changed` 信号驱动（与农场/订单其它屏的响应式范式对齐）。
> ✅ 已优化（`3960e1f`）：删除每帧 `_process` 轮询，改为 `_ready` 中 `GameManager.arrived_changed.connect(_on_arrived_changed)` 订阅；`_on_arrived_changed` 调用 `_refresh()` 刷新「已到货 N 件」提醒，`_ready` 末尾仍 `_refresh()` 一次覆盖跨会话已到货初始显示；并补 `_exit_tree` 在节点重建时断开信号避免重复连接。到货（`_tick_orders`）与领取（`confirm_receipt_all` / `confirm_receipt_one`）均已 emit `arrived_changed`，全路径覆盖，无遗漏。

**10. `product_catalog.gd:174` / `receipt_panel.gd:85` 忽略 `start_order` / `confirm_receipt` 返回值（✅已提交推送 a862615）**
失败仍 `queue_free()` 关闭。检查返回值，失败给提示而非静默关闭。

**11. `inspiration_panel.gd:128` `start_activity` 返回值未校验（✅已提交推送 a862615）**
若 GM 拒绝（已有活动进行）UI 仍 `_show_running()`，状态不一致。`start_activity` 应返回 bool 并检查。

**12. `clothes/align_tool.gd` 引用已删字段 `equip_offset`**
脚本解析失败，工具已损坏。删除或重写为共享画布预览工具。
> ✅ 已优化（`ad3f022`）：经全仓引用核查，确认 `align_tool` 为已废弃开发工具——它硬编码 `load("res://data/clothes_*.tres")` 5 个文件（现均已不存在）、引用 `ClothesData.equip_offset`（该字段已在「服装共享画布原点、叠加即对齐」重构中删除），且全仓运行时零引用（仅 `align_tool.tscn` 自引用）。同目录的 `item_slot.gd`/`equip_target.gd` 是活的换装运行时组件（`wardrobe.gd:27` 预加载 `item_slot.tscn`、`wardrobe.tscn` 引用 `equip_target.gd`），予以保留。故删除 `align_tool.gd` + `.uid` + `align_tool.tscn` 三件套。

**13. `arrow_nav.gd:30` `setup()` 无幂等保护（✅已提交推送 a862615）**
重复调用重复 `connect(screen_changed)`。加 `_initialized` 守卫或先 `disconnect`。

**14. `focus_bar.gd:14` `@onready var _title` 无赋值表达式却手动 new（误导）+ `:124` `await 0.9s` 后未加 `is_instance_valid(self)` 守卫**
节点期间 `queue_free` 会报错。
✅ **已优化（`f557f77`）**：5 个 `@onready var` 声明去掉 `@onready` 前缀（子节点均在 `_build_children()` 手动 `new()`，非场景树获取，原标记与事实相反纯属误导，并加注释说明）；`_on_interrupted` 在 `await create_timer(0.9)` 恢复后补 `if not is_instance_valid(self): return` 守卫，避免等待期间专注条被 `queue_free`（切场景/收起）后访问已释放实例。

---

## 四、P2（可选优化，合并列举）

### 重复代码（✅ 已执行，待提交）
- `_format_time` 四处、`_make_icon` 三处、phone/clothing_rack 拖拽状态机、pet/customer `contains_point` 样板 → 各抽公共函数 / 基类。
- **落地**：新增 `scripts/utils.gd`（`class_name Utils`，无状态静态工具），提供 `format_time` / `make_icon` / `point_in_area` / `exceeds_drag_threshold` 四个方法；原 4 处 `_format_time`、3 处 `_make_icon` 全部改为 `Utils.*` 并删除本地重复定义；pet/customer 物理 `contains_point` 统一走 `Utils.point_in_area(global_pos, area, mask)`；phone/clothing_rack/pet 三处 `distance_to(...) >= DRAG_THRESHOLD` 统一走 `Utils.exceeds_drag_threshold`，并删除三处冗余 `const DRAG_THRESHOLD`（默认 4px 收口到 Utils）。

### 屏生命周期（✅ 已执行，待提交）
- **`wardrobe.gd:215` `_on_close → queue_free()`** — wardrobe 现是 ScreenManager 常驻切屏，点 CloseButton 会销毁整个屏（CloseButton 已 hide，但留雷）。改 `visible=false` 或删该方法。
- 多处 `queue_free` 子节点未先 `remove_child`（`wardrobe.gd:72` 等）— 本帧仍在校验内，范式不统一（`workshop_screen.gd:248` 是正确范式）。
- **落地**：`wardrobe._on_close` 由 `queue_free()` 改为 `visible = false`（销毁常驻屏的雷已消除）；`_populate_backpack` 清空网格改为先 `remove_child` 再 `queue_free()`，与 `workshop_screen.gd:248` 范式统一。

### 硬编码（✅ 已执行，待提交）
- `placement_manager.gd:51` 中心 `(340,240)` / `POT_ID="pot"`
- `product_catalog` / `receipt_panel` 的 `res://data/product_*.tres` 回退路径
→ 常量化 / `@export` 注入。

### 性能（✅ 已执行，待提交）
- `placement_manager.gd:58` 每次 release 写盘 → 摆放物多时卡顿，节流（如 0.5s 合并）。
- `customer.gd` String 状态机（idle/shopping/...）应改 enum；`browsing` 离开也 `emit order_completed`，信号名误导。
- **落地**：
  - `placement_manager` 新增 `Timer` 节流（`wait_time=0.5`、`one_shot`），`_on_placed_moved` 改为 `_request_save()`（拖拽中连续移动只合并落盘一次）；`spawn`/`remove` 这类低频操作仍即时 `save_placements()`；并补 `_exit_tree` 在退出前补齐未触发的节流存档，避免最后 0.5s 移动丢失。
  - `customer.gd` 状态机由 String（`"idle"/"shopping"/"browsing"/"leaving"`）改为 `enum State { IDLE, SHOPPING, BROWSING, LEAVING }`，所有比较/赋值改类型安全枚举。关于「`browsing` 离开也 emit `order_completed` 信号名误导」：该信号实为「顾客离场（无论购买与否），用于驱动 Main 下一个顾客生成循环；金币已在 `sell_from_rack` 结算」；为避免改动跨文件连接（`main.gd:328`）引入回归，保留信号名，仅在注释中澄清语义（如需彻底改名可后续单独做）。

### 类型 / 数据设计（✅ 已执行，待提交）
- `item_data.gd:27` 注释「仅四类」与枚举 5 值（含 DECOR）不符。
- `clothes_data` 继承的 `icon` 与自有 `icon_texture` 语义重叠。
- `pet.gd` `@export` 与 PetData 双重声明冗余；`_position_at_edge` 的 top/bottom 分支死代码；pet 用 local position 判界 vs customer 用 global_position 不一致。
- `customer_data` / `pet_data` `region_rect` 默认 `Rect2()`（空）— 漏填则贴图不可见，默认应给安全值。

### 存档 / 其它（✅ 已执行，待提交）
- `cfg.save()` 返回值多处未检查。
- 缺统一存档版本号 / 迁移框架（迁移逻辑散落）。
- `receipt_panel` 未监听 `arrived_changed`，开着时新到货不刷新。
- `inspiration_panel:232` 字符串跨层调 `Main._enter_focus_mode` → 改信号。

---

## 五、修复优先级建议

| 优先级 | 内容 | 改动量 | 风险 |
|---|---|---|---|
| **① 立刻** | P0 placement_manager 复活 bug | 1~2 行 | 零风险 |
| **② 低成本高收益 P1** | hud tween kill、pet 初始化顺序、arrow_nav 幂等、各返回值校验（#6.#7.#13.#10.#11） | 各数行 | 低 |
| **③ 中等投入 P1** | craft/decompose 批量模式 + UI getter 收口（#3.#4.#5.#8） | 局部重构 | 中（需无头验证） |
| **④ 长期架构** | 拆分上帝单例 + 抽公共工具函数（#2 + P2 重复代码） | 架构级 | 高（建议结合「种植系统演进为花园屏」重构一起做） |

**建议起点**：从 **① + ②**（约 7 处，都在几行内）开始，一次性改完用无头模式验证；③④ 视后续重构计划排期。

---

## 六、修复进度跟踪

### 6.1 已完成（已提交推送 `a862615`）

| 项 | 文件 | 修复要点 | 提交状态 |
|---|---|---|---|
| **P0 #1** 摆放物复活 | `placement_manager.gd` | `save_placements` 跳过 `is_queued_for_deletion()` 节点；`restore_placements` 加 `has_section` 守卫 | ✅ a862615 已推送 |
| **P1 #6** tween 抖动 | `hud.gd` | 建新 tween 前 `if is_instance_valid(_tween): _tween.kill()` | ✅ a862615 已推送 |
| **P1 #7** 宠物瞬灭 | `pet.gd` | `_ready` 先 `_apply_visuals()` 再 `_pick_direction()` | ✅ a862615 已推送 |
| **P1 #10** 订单返回值 | `product_catalog.gd` / `receipt_panel.gd` / `game_manager.gd` | `confirm_receipt()` 升级为 `bool`；两处调用校验失败不静默 `queue_free()` | ✅ a862615 已推送 |
| **P1 #11** 活动返回值 | `game_manager.gd` / `inspiration_panel.gd` | `start_activity()` 升级为 `bool` + null/已运行双守卫；UI 拒绝时不进入进行中视图 | ✅ a862615 已推送 |
| **P1 #13** 重复订阅 | `arrow_nav.gd` | `setup()` 加 `_initialized` 幂等守卫 | ✅ a862615 已推送 |
| 用户追加：弹窗不阻断拖动 | `main.gd` | 新增 `_has_any_overlay()`；统一拖动手柄 = 顶部拖拽带 × 灵感标题栏；任意弹窗打开时窗口顶边仍可拖 | ✅ a862615 已推送 |
| 用户追加：变量重名 | `main.gd` | `_spawn_pet` 内层 `interval` 重命名为 `retry_interval`，消除 `CONFUSABLE_LOCAL_DECLARATION` | ✅ a862615 已推送 |

> 说明：以上 8 处已在提交 `a862615`（含 `.gitignore` 规则 `a232a39`）推送 `origin/main`，工作树干净，本机无 Godot 仅人工审查。

### 6.1b 上帝单例拆分 P1 #2（已完成 · 6/6 子管理器 · 均提交推送 `origin/main`）

门面式渐进拆分：`GameManager` 在 `_ready` 实例化各子管理器并 `add_child`、设 `owner_mgr = self` 反向引用处理跨域、连接子管理器信号做 re-emit；`GameManager` 暴露只读转发属性 + 委托方法，全代码库 `GameManager.xxx` 调用点零改动。订单与蓝图注册表仍留在 `GameManager`（因 `InventoryManager.craft` 反向引用蓝图）。

| 项 | 文件（新增） | 迁出职责 | 提交 |
|---|---|---|---|
| **InventoryManager** | `scripts/inventory_manager.gd`（+ `.uid`） | 物品注册表、统一库存、分解、制作（经 `owner_mgr` 查 GM 蓝图）、解锁集合、起始播种 flag、`load_all()`、`inventory_changed` | `9e387b7` ✅ |
| **EconomyManager** | `scripts/economy_manager.gd`（+ `.uid`） | `gold` / `inspiration`（setter 发 `currency_changed`）/ `inspiration_total_earned` 存档 / `add/subtract_gold` / `add/subtract_inspiration` / `complete_order`；`add_inspiration` 经 `owner_mgr._evaluate_blueprint_unlocks` 触发蓝图解锁 | `724ac53` ✅（含补交 `economy_manager.gd.uid`） |
| **ActivityManager** | `scripts/activity_manager.gd`（+ `.uid`） | 番茄钟墙钟计时、OUTING 外出键鼠统计（GlobalInput）、专注条、连击 streak、打断计数/衰减结算、离线补发；GlobalInput 节点由 AM 自管、退出 `stop_hook` | `11972e0` ✅ |
| **WardrobeManager** | `scripts/wardrobe_manager.gd`（+ `.uid`） | 穿搭真相源 `equipped`、`equip/unequip`、`get_equipped/get_worn_count/is_worn`、穿搭存档、旧 `wardrobe.cfg` 一次性迁移 | `2335900` ✅ |
| **RackManager** | `scripts/rack_manager.gd`（+ `.uid`） | 展架真相源 `clothing_rack`、上架/下架、可售库存查询、顾客购买结算、可上架列表、展架存档（含历史重复槽清理）；经 `owner_mgr` 调 `get_count/get_item/add_gold/remove_item/get_by_category` | `e78ddf1` ✅ |
| **FarmManager** | `scripts/farm_manager.gd`（+ `.uid`） | 农场槽真相源 `farm_slots`、放盆/种/生长阶段/成熟判定/采摘/清空槽、墙钟挂机续算、农场存档；经 `owner_mgr` 调 `get_item/has_item/remove_item/add_item`（注：`seed_starter_farm` 起始播种入口故意留 GM，写 `inventory_mgr.farm_seeded`） | `74010f9` ✅ |

> **RackManager 编译错误修复**：`get_displayable_clothing()` 中 `var avail := owner_mgr.get_count(id) if owner_mgr != null else 0` 因 `owner_mgr` 是基类 `Node`、编译期不知 `get_count` 返回 int，三元结果退化为 `Variant` 致 `:=` 无法推断确定类型 → 改为显式 `var avail: int = ...`。已提交 `b68d8cd` ✅。
>
> 校验方式（每个子管理器）：全 `scripts/` grep 确认私有字段/方法已彻底迁出 GM（零残留）；外部调用点全部经转发层零改动；委托 API / 信号名与原文一致。本机无 Godot，均为人工静态审查。

### 6.1c P1 #3 批量写盘优化（已完成 · ✅ `6f605ac`）

`craft` / `decompose` 一次操作含「N 次 remove + 1 次 add」（或 M 次 add + 1 次 remove），原实现每次 `add/remove` 都 `_save_inventory()` + `inventory_changed.emit()`，触发 **N+1 次写盘 + N+1 次广播**。

| 项 | 文件 | 修复要点 | 提交 |
|---|---|---|---|
| **P1 #3** 批量写盘 | `scripts/inventory_manager.gd` / `game_manager.gd` | 新增 `begin_batch()/commit_batch()`（depth 计数支持嵌套）+ `_notify_inventory_changed()`；`add_item/remove_item` 在批量期间只置脏不落盘不广播；`craft`/`decompose` 整体包在批量段内，落盘与 `inventory_changed` 广播各仅一次。`GameManager` 门面转发 `begin_batch/commit_batch`，便于未来多步批量操作复用 | `6f605ac` ✅ |

> 校验（人工静态）：`inventory_manager.gd` 内 `inventory_changed.emit()` 与 `save_inventory()` 的触发点现仅剩 `_notify_inventory_changed()` 与 `commit_batch()` 两处；非批量调用（订单到货、农场收获、展架售出等）仍即时落盘+广播，行为不变；批量失败早返回时不产生多余写盘（depth 归零但 `_batch_dirty` 为假）。

### 6.1d P1 #4 UI 直读 GM 内部字段 getter 收口（已完成 · ✅ `2b66ea8`）

UI 此前直接读取 `GameManager` 的 4 个集合属性（`farm_slots` / `unlocked_clothes` / `equipped` / `clothing_rack`），违反低耦合。本次补 getter 收口：

| 项 | 文件 | 修复要点 | 提交 |
|---|---|---|---|
| **P1 #4** getter 收口 | `scripts/{farm,inventory,wardrobe,rack}_manager.gd` / `game_manager.gd` / `farm_screen.gd` / `wardrobe.gd` / `workshop_screen.gd` / `rack_panel.gd` | 新增 `get_slot` / `get_unlocked_clothing_ids` / `get_equipped_dict` / `get_rack_count`，分别在 Farm/Inventory/Wardrobe/Rack 子管理器实现，`GameManager` 转发；4 处 UI 直读改为走 getter；原 4 个集合属性降级为兼容 shim。`is_blueprint_unlocked(id)` 已存在（`game_manager.gd:561`），仅改 `workshop_screen` 的 `unlocked_blueprints.has()` 直读 | `2b66ea8` ✅ |

> 校验（人工静态）：全 `scripts/` 已无 UI 直读 `GameManager.equipped/unlocked_clothes/farm_slots/clothing_rack` 属性的代码（仅剩数据模型说明性注释）；4 个 getter 在子管理器实现 + GM 转发，行为等价。

### 6.1e P1 #5 decompose 弱类型改 `Array[MaterialCost]`（已完成 · ✅ `adb797e`）

`decompose_recipe` 此前用 `Array[Dictionary]` + id 字符串，每次分解都要重建 `norm` 字典处理 String/StringName 键不一致；与 `BlueprintData.required_materials` 的强类型 `Array[MaterialCost]` 不一致。

| 项 | 文件 | 修复要点 | 提交 |
|---|---|---|---|
| **P1 #5** 强类型化 | `scripts/resources/item_data.gd` / `scripts/inventory_manager.gd` / `scripts/workshop_screen.gd` / `data/crops/crop_rose.tres` | `item_data.gd` 加 `const MaterialCost = preload(...)`（泛型参数需在本作用域可见），`decompose_recipe` 改 `Array[MaterialCost]`；`_decompose_internal` 删掉 dict 归一化、直接遍历 `MaterialCost`（`mc.item.id` / `mc.count`）；`_recipe_text` 直读 `mc.item.display_name` 不再按 id 查表；`crop_rose.tres` 字典数组重写为 `Array[MaterialCost]` 子资源（仿 `blueprint_scarf.tres`，`material_cost.gd` uid `uid://bfamwjglm7r2a`）；空 `[]` 的 `fiber.tres` / `red_dye.tres` 无需改动 | `adb797e` ✅ |

> 校验（人工静态）：全代码库 grep，`decompose_recipe` 已无任何字典式访问（`.item_id` / `norm[` 清零）；`material_cost.gd` 仅反向引用 `ItemData` 类型、不预加载 `item_data.gd`，故 `item_data.gd` 预加载 `material_cost.gd` 不构成循环依赖（与 `BlueprintData` 引用 `ItemData` 同机制，已验证可解析）。

### 6.1f P1 #8 衰减系数与活动逻辑重复（已完成 · ✅ `474e180`）

`maxf(0.5, 1-n*0.2)` 魔法数此前在 `inspiration_panel.gd` 预览与 `activity_manager.gd` 结算双处维护，改一处易漏另一处。

| 项 | 文件 | 修复要点 | 提交 |
|---|---|---|---|
| **P1 #8** 衰减系数收口 | `scripts/activity_manager.gd` / `scripts/game_manager.gd` / `scripts/inspiration_panel.gd` | `activity_manager.gd` 抽 `get_reward_factor()` 为单一真相源，加 `REWARD_DECAY_MIN`/`REWARD_DECAY_STEP` 常量；`_finish_activity` 结算与 `inspiration_panel._update_countdown_label` 预览都走它；`game_manager.gd` 门面转发 `get_reward_factor()`，UI 删魔法数 | `474e180` ✅ |

> 校验（人工静态）：全 `scripts/` grep，`maxf(0.5, 1.0 - ...*0.2)` 衰减公式现仅存在于 `activity_manager.gd:get_reward_factor()` 一处；结算（`_finish_activity` 调 `get_reward_factor()`）与 UI 预览（`GameManager.get_reward_factor()`）同源，行为等价。

### 6.1g P1 #9 phone 每帧轮询改订 `arrived_changed` 信号（已完成 · ✅ `3960e1f`）

`scripts/phone.gd`（桌面电话摆件）原在 `_process` 每帧调用 `_refresh()` 轮询 `GameManager.get_arrived().size()` 来刷新「已到货 N 件 · 单击查看」提醒，与农场/订单其它屏的响应式范式不一致。

| 项 | 文件 | 修复要点 | 提交 |
|---|---|---|---|
| **P1 #9** 信号驱动 | `scripts/phone.gd` | 删除每帧 `_process` 轮询；`_ready` 中 `GameManager.arrived_changed.connect(_on_arrived_changed)` 订阅，`_on_arrived_changed` 调 `_refresh()` 刷新提醒；`_ready` 末尾保留一次 `_refresh()` 覆盖跨会话已到货的初始显示；补 `_exit_tree` 在节点重建时断开信号避免重复连接 | `3960e1f` ✅ |

> 校验（人工静态）：到货（`game_manager.gd:_tick_orders` emit `arrived_changed`）、领取（`confirm_receipt_all` / `confirm_receipt_one` 均 emit `arrived_changed`）全路径覆盖，提醒刷新无遗漏；初始显示由 `_ready` 的 `_refresh()` 兜底，行为等价且零每帧开销。

### 6.1h P1 #12 删除已废弃损坏的 `align_tool` 开发工具（已完成 · ✅ `ad3f022`）

`scripts/clothes/align_tool.gd` 原是「衣物可视化对齐工具」（编辑器 F6 运行的开发期工具），依赖已删除的 `ClothesData.equip_offset` 字段，且硬编码 `load("res://data/clothes_*.tres")` 5 个文件（该批 .tres 在「服装并入统一 inventory」重构后已不存在），故运行即报错、工具已损坏。

| 项 | 文件 | 修复要点 | 提交 |
|---|---|---|---|
| **P1 #12** 删除废弃工具 | `scripts/clothes/align_tool.gd` + `.uid` / `scenes/clothes/align_tool.tscn` | 全仓引用核查确认为零运行时引用（仅 `align_tool.tscn` 自引用）；同目录 `item_slot.gd`/`equip_target.gd` 为活的换装运行时组件（被 `wardrobe.gd`/`wardrobe.tscn` 引用），保留。删除 `align_tool` 三件套 | `ad3f022` ✅ |

> 校验（人工静态 + 引用图）：grep 全仓（含 `.gd`/`.tscn`/`.godot`/`.cfg`）确认 `align_tool` 无运行时引用；`item_slot`/`equip_target` 由 `wardrobe.gd:27`、`scenes/wardrobe.tscn:4` 引用，属在用组件未动。删除后仓库无悬空引用。
> ⚠️ **操作坑（环境相关）**：本机 `git rm` 在本 Bash 隔离文件系统下会触发工作树级联删除（一次 `git rm` 3 个文件会误删约 110 个跟踪文件），索引始终完好、可用 `git restore --worktree .` 完整恢复。本次改用 `rm` 删文件 + `git add` 暂存删除，规避该陷阱。

### 6.1i P1 #14 focus_bar 去误导性 `@onready` + `await` 后补 `is_instance_valid` 守卫（已完成 · ✅ `f557f77`）

`scripts/focus_bar.gd` 两个隐患：

1. **第 14–18 行 `@onready` 误导**：5 个节点变量声明带 `@onready` 却又无赋值表达式，实际子节点均在 `_build_children()` 中手动 `Label.new()`/`ProgressBar.new()`/`Button.new()` 创建，`@onready`（从场景树自动获取子节点）与事实相反，纯属误导阅读。去掉 `@onready` 前缀并加注释说明。
2. **第 128 行 `await` 后缺守卫**：`_on_interrupted` 在 `await get_tree().create_timer(0.9).timeout` 等待期间，若专注条被 `queue_free`（切场景 / Main 收起专注条）而销毁，协程恢复后会访问已释放实例的方法报错。在 `await` 后补 `if not is_instance_valid(self): return` 守卫。

| 项 | 文件 | 修复要点 | 提交 |
|---|---|---|---|
| **P1 #14** 弃用误导声明 + 协程守卫 | `scripts/focus_bar.gd` | `@onready var _title/_countdown/_progress/_toggle/_cancel_btn` 去 `@onready`（改普通 `var` + 注释）；`_on_interrupted` 在 `await` 恢复后补 `is_instance_valid(self)` 守卫避免 `queue_free` 后访问已释放实例 | `f557f77` ✅ |

> 校验（人工静态）：去除 `@onready` 不改变运行时语义（变量本即在 `_build_children()` 内赋值，`@onready` 无初始化表达式时行为等价）；`_toggle != null` / `_cancel_btn` 等既有空检查保留，不受影响。`is_instance_valid(self)` 在 `queue_free` 后返回 false，协程提前返回，不再触碰 `_title`/`add_theme_stylebox_override`/`_refresh_title`。

### 6.2 待办

**P1 已全部完成（13/13）** ✅。

**P2 已全部完成（15/15）** ✅：①②④ 提交 `0edb305`（见 6.4），③⑤⑥ 提交 `0fc7c9d`（见 6.5），存档版本框架推广至全部存档（见 6.6）。

### 6.3 后续动作

**P0 + P1（13 项）+ P2（15 项）全部完成**。P2 第一批（①②④）提交 `0edb305`，第二批（③⑤⑥）提交 `0fc7c9d`，存档版本框架推广（见 6.6）为收官项。剩余工作聚焦点：

- [ ] **终极联调实测（Godot 4.7）**：6 个子管理器（库存/经济/活动/穿搭/展架/农场）联调无回归——重点农场挂机续算、各信号广播、旧档迁移、专注条打断闪烁协程。
- [x] **P2 存档版本号落地推广**（✅ 见 6.6）：全部 12 个存档（10 个 ConfigFile + 2 个 JSON）已统一写入 `save_version`，读取侧均有旧档迁移入口；未来任何存档格式变更只需 `SAVE_VERSION+1` 并在对应迁移入口填逻辑。

### 6.4 P2 第一批完成（①②④ · ✅ 已提交推送 `0edb305`）

统一新增 `scripts/utils.gd`（`class_name Utils`）+ `utils.gd.uid`；`rm -rf .godot && --headless --editor --quit` 重建类缓存，**0 解析 / 0 SCRIPT ERROR**（已过滤 GDExtension 无头噪声）。改动文件与要点：

| 类 | 文件 | 修复要点 |
|---|---|---|
| **① 重复代码** | `scripts/utils.gd`（新增） | `format_time` / `make_icon` / `point_in_area` / `exceeds_drag_threshold` 四个无状态静态方法 |
| | `focus_bar.gd` / `inspiration_panel.gd` / `phone_panel.gd` | 3 处 `_format_time` → `Utils.format_time`，删本地定义 |
| | `phone_panel.gd` / `product_catalog.gd` / `receipt_panel.gd` | 3 处 `_make_icon` → `Utils.make_icon`，删本地定义 |
| | `pet.gd` / `customer.gd` | 物理 `contains_point` 抽 `Utils.point_in_area(global_pos, area, mask)` |
| | `phone.gd` / `clothing_rack.gd` / `pet.gd` | 拖拽阈值判定抽 `Utils.exceeds_drag_threshold`；删三处冗余 `const DRAG_THRESHOLD` |
| **② 屏生命周期** | `wardrobe.gd` | `_on_close` 由 `queue_free()` → `visible = false`（消除销毁常驻屏的雷）；`_populate_backpack` 清空网格先 `remove_child` 再 `queue_free()`（与 `workshop_screen.gd:248` 范式统一） |
| **④ 性能** | `placement_manager.gd` | 新增 `Timer` 节流（0.5s 合并落盘），`_on_placed_moved` → `_request_save()`；`spawn`/`remove` 仍即时存；补 `_exit_tree` 退前补齐节流存档 |
| | `customer.gd` | 状态机 String → `enum State { IDLE, SHOPPING, BROWSING, LEAVING }`（全比较/赋值类型安全） |

> 校验方式：全仓 grep 确认 `_format_time` / `_make_icon` 本地定义与调用均已清零，`DRAG_THRESHOLD` 仅存于 `utils.gd` 注释；`customer.gd` 旧状态字符串零残留；`Utils` 因 `class_name` 全局可直接调用，调用点无需 autoload。本机无 Godot 仅人工/无头审查+解析校验。

### 6.5 P2 第二批完成（③⑤⑥ · ✅ 已提交推送 `0fc7c9d`）

统一 `rm -rf .godot && --headless --editor --quit` 重建类缓存 + `--headless` 实跑 15s，**0 SCRIPT ERROR / 0 Runtime Error**（仅无头退出时的 RID 泄漏噪声，与项目无关）。改动文件与要点：

| 类 | 文件 | 修复要点 |
|---|---|---|
| **③ 硬编码** | `scripts/placement_manager.gd` | 出生中心 `(340,240)` 收口为 `const DEFAULT_SPAWN_CENTER`；`POT_ID` 改为由 `init(pot_id=)` 注入（Main 按 `garden_placement` 推导，兜底 `"pot"`），限量判定改用 `_pot_id` |
| | `scripts/product_catalog.gd` / `scripts/receipt_panel.gd` | 回退路径 `["res://data/product_chair.tres", ...]` 收口为 `const DEFAULT_PRODUCT_PATHS` |
| | `scripts/main.gd` | 调 `placement_manager.init(..., pot_id_for_seed)` 传入运行时花盆 id；删除 `icon_texture→icon` 旧迁移块（字段已移除） |
| **⑤ 类型/数据** | `scripts/resources/item_data.gd` | `category` 注释「仅四类」→ 明确 CLOTHING/SEED/CROP/MATERIAL/DECOR 五类 |
| | `scripts/resources/clothes_data.gd` + `data/clothes_{hat,shirt,skirt,shoe,gt}.tres` | 删除冗余 `icon_texture` 字段，统一复用基类 `icon`；5 个 .tres 的 `icon_texture=` 改为 `icon=` |
| | `scripts/clothes/item_slot.gd` | 预览/拖拽图标改读 `_data.icon`（单一真相源） |
| | `scripts/pet.gd` | 行走参数 `@export` → 普通 `var`（PetData 为唯一真相源，仅无数据兜底）；删除 `_position_at_edge` 的 `top/bottom` 死分支；移动/边界判定由局部 `position` 改为 `global_position`（与 customer 一致） |
| | `scripts/resources/customer_data.gd` / `scripts/resources/pet_data.gd` | `region_rect` 默认 `Rect2()` → `Rect2(0,0,736,414)` 安全值（漏填也不致贴图不可见） |
| **⑥ 存档/其它** | `scripts/receipt_panel.gd` | 订阅 `GameManager.arrived_changed` → `_populate()`，开着时新到货实时刷新；`_exit_tree` 断开避免重复连接 |
| | `scripts/inspiration_panel.gd` / `scripts/main.gd` | 「最小化专注条」跨层字符串调 `Main._enter_focus_mode` → 改为 `signal request_minimize`，`_open_inspiration` 中 `panel.request_minimize.connect(_enter_focus_mode)` |
| | `scripts/utils.gd` | 新增统一存档版本框架起步：`const SAVE_VERSION`、`write_save_version` / `read_save_version` / `is_legacy_save` |
| | `scripts/placement_manager.gd` | `save_placements` 写入版本戳并校验 `cfg.save()` 返回值；`restore` 走 `Utils.is_legacy_save` 迁移占位分支 |
| | `scripts/{activity,economy,farm,inventory,rack,wardrobe}_manager.gd` / `scripts/{phone,clothing_rack,game_manager}.gd` | 全部 `cfg.save()` 调用补 `!= OK` 返回值检查 + `push_warning` 诊断 |

> 校验方式：全仓 grep 确认 `icon_texture` 引用已清零（仅剩文档提及），`pet.gd` 旧 `top/bottom` 分支与局部 `position` 判界零残留；`DEFAULT_SPAWN_CENTER` / `DEFAULT_PRODUCT_PATHS` 常量接管原魔法串；各 `cfg.save(` 均有返回值守卫。无头解析 + 15s 实跑零项目级错误。
>
> 后续推广：统一存档版本框架已在 `Utils` 落地，建议其余 5+ 个存档（inventory / equipped / rack / farm / blueprints / activity / phone / clothing_rack）按 `placement_manager` 同模式补版本戳与迁移分支，实现「统一存档版本 + 迁移」闭环。（✅ 已于 6.6 完成）

### 6.6 存档版本框架推广至全部存档（P2 收官 · 待提交）

`Utils.SAVE_VERSION` 版本框架从 `placement_manager` 推广到项目**全部 12 个存档**（10 个 ConfigFile + 2 个 JSON），统一「写入版本戳 + 读取侧旧档迁移入口」闭环。校验：`rm -rf .godot && --headless --editor --quit` 0 解析错误；`--headless` 实跑 15s 0 运行期错误；实际存档抽查确认 `save_version=1` 已落盘（inspiration_total / inventory / unlocked_clothes / phone_orders.json）。

| 存档 | 文件 | 改动 |
|---|---|---|
| 连击 streak.cfg | `activity_manager.gd` | save 前 `Utils.write_save_version`；load 加 `is_legacy_save` 迁移入口 |
| 活动 inspiration_active.json | `activity_manager.gd` | JSON payload 增 `save_version` 键；load 加版本比对入口；写失败补 `push_warning` |
| 展架位置 | `clothing_rack.gd` | save 前写版本戳（位置存档 v0≡v1，读取侧无需迁移入口） |
| 灵感累计 | `economy_manager.gd` | 写版本戳 + load 迁移入口 |
| 农场 | `farm_manager.gd` | 写版本戳 + load 迁移入口 |
| 蓝图解锁 | `game_manager.gd` | 写版本戳 + load 迁移入口 |
| 订单 phone_orders.json | `game_manager.gd` | JSON payload 增 `save_version` 键；load 加版本比对入口；写失败补 `push_warning` |
| 库存 inventory.cfg | `inventory_manager.gd` | 写版本戳（与 seeded 标记同住 `[meta]` 段，键不冲突）+ load 迁移入口 |
| 已解锁服装 | `inventory_manager.gd` | 写版本戳 |
| 电话位置 | `phone.gd` | 写版本戳（位置存档同上） |
| 展架槽位 | `rack_manager.gd` | 写版本戳 + load 迁移入口 |
| 穿搭 equipped | `wardrobe_manager.gd` | 写版本戳 + load 迁移入口 |

> 使用约定：未来任何存档格式变更 → `Utils.SAVE_VERSION` +1，然后在对应存档的「旧档迁移入口」（现为 `if Utils.is_legacy_save(cfg): pass` 占位）填入 v(n-1)→v(n) 转换逻辑即可；JSON 存档用 `parsed.get("save_version", 0)` 同理。`placement_manager` 为最早范例（6.5）。
